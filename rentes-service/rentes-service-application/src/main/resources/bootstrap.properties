#####################################################################
# Configuration Spring de base
#####################################################################
# app name
spring.application.name=rentes-service

# active profile
spring.profiles.active=dev
#dev h2
#spring.profiles.active=dev

#config branche
spring.cloud.config.label=rentes-service
# config uri
spring.cloud.config.uri=http://localhost:8100/config

# url format : /{apname}/{profile}/{label}/{filename}
logging.config=http://localhost:8100/config/rentes-service/default/rentes-service/logback-spring.xml